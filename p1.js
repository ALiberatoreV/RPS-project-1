var rounds = prompt("How many rounds");
var compChoice = math.floor(Math.random() * 3);
var r = parseInt(rounds) + "<br>"
for(let i = 0; <= rounds; i++)
var player = prompt("Pick one R / P / S ");

if(compChoice = 0){
    compChoice = "rock";
} else if(1 <= compChoice <= 2){
    compChoice = "paper";
} else{
    compChoice = "scissors";
}

       